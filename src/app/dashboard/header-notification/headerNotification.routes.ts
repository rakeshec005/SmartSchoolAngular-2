import { Route } from '@angular/router';
import { headerNotificationComponent } from './index';

export const headerNotificationRoutes: Route[] = [{
    path: 'headerNotification',
     component: headerNotificationComponent
    }];
