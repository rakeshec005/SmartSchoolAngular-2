import { Route } from '@angular/router';
import { ListUserPlanComponent } from './index';

export const ListUserPlanRoutes: Route[] = [{
    path: 'ListUserPlan',
     component: ListUserPlanComponent
    }];
